@model ZADALKHAIR.Models.FeedBack

<div class="modal-header">
    <div class="row px-2" style="flex-direction: column">
        <h3 class="modal-title" id="exampleModalLongTitle">@Model.Name</h3>
        <span>@Model.Email</span><br />
        <span>@Model.CreatedAt.ToString().Split(" ")[0]</span>
    </div>
    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
        <span aria-hidden="true">&times;</span>
    </button>
</div>
<div class="modal-body">
    @Model.Massege
</div>
<div class="modal-footer">
    <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
</div>
